import { EnderecoGeolocalizacao } from './endereco-geolocalizacao';

describe('EnderecoGeolocalizacao', () => {
  it('should create an instance', () => {
    expect(new EnderecoGeolocalizacao()).toBeTruthy();
  });
});
