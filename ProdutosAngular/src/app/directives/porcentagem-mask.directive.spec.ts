import { PorcentagemMaskDirective } from './porcentagem-mask.directive';

describe('PorcentagemMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new PorcentagemMaskDirective();
    expect(directive).toBeTruthy();
  });
});
